# Nixpacks build config for Railway.
# Pins Node to a specific major so builds are reproducible across deploys.
# Nixpacks auto-detects the install/build/start flow from package.json,
# so we only override what we care about (Node version).

[phases.setup]
nixPkgs = ["nodejs_22"]
